package src.com.java.TankBattle;

public class MyTank extends Tank {
    public MyTank(int x, int y, int color, boolean visible, int life, int size, int speed, int direction, boolean alive, int bulletNum){
        super(x, y, color, visible, life, size, speed, direction, alive, bulletNum);
    }
}
